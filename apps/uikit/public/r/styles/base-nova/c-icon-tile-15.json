{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "c-icon-tile-15",
  "type": "registry:block",
  "title": "Soft tones",
  "description": "Tone-adjustable soft variant",
  "categories": [
    "icon-tile"
  ],
  "registryDependencies": [
    "https://uikit.limmengty.com/r/styles/base-nova/icon-placeholder.json",
    "https://uikit.limmengty.com/r/styles/base-nova/icon-tile.json",
    "https://uikit.limmengty.com/r/styles/base-nova/styles.json"
  ],
  "dependencies": [],
  "files": [
    {
      "path": "registry/bases/base/components/icon-tile/c-icon-tile-15.tsx",
      "type": "registry:block",
      "content": "import { IconTile } from \"@/components/reui/icon-tile\"\n\nimport { IconPlaceholder } from \"@/components/ui/icon-placeholder\"\n\n// The `soft` variant derives every fill and border from currentColor, so one\n// text color class retints the whole tile. An empty class keeps the primary\n// default. Each tone uses its own base color as text (not the `-foreground`\n// token, which is near-white and meant for text on a solid fill, not a\n// light tint) so it stays readable in light mode; dark mode brightens it.\nconst tones = [\n  \"\",\n  \"text-info dark:text-info\",\n  \"text-success dark:text-success\",\n  \"text-warning dark:text-warning\",\n  \"text-destructive dark:text-destructive\",\n]\n\nexport default function Pattern() {\n  return (\n    <div className=\"flex items-center justify-center gap-4\">\n      {tones.map((tone) => (\n        <IconTile\n          key={tone || \"primary\"}\n          variant=\"soft\"\n          className={tone}\n          aria-hidden=\"true\"\n        >\n          <IconPlaceholder\n            lucide=\"StarIcon\"\n            tabler=\"IconStar\"\n            hugeicons=\"StarIcon\"\n            phosphor=\"StarIcon\"\n            remixicon=\"RiStarLine\"\n          />\n        </IconTile>\n      ))}\n    </div>\n  )\n}\n",
      "target": "components/examples/c-icon-tile-15.tsx"
    }
  ],
  "meta": {
    "order": 15,
    "gridSize": 2
  }
}